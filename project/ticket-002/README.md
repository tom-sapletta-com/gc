# ticket-002 — Documentation placement

Status: IN_PROGRESS
Phase: PUBLICATION

SESSION_EXECUTION_AUTHORIZATION: user requested autonomous documentation placement updates across all repositories and publication. GitHub issue #2 allocates the legacy identity before implementation; no managed allocator exists. Worktree starts at origin/main and preserves unknown primary changes.

Outcome: canonical agent instructions, index, immutable standard pin, and CI verifying existing Python tests and documentation placement. Protected Validator-agent publication remains required; a mismatched trusted check profile must be resolved independently.

Validation: existing Python tests, pinned docs checker, index links and workflow structure. Canonical result: [documentation index](../../docs/README.md).

Validation completed: 57 existing Python tests pass. Pinned docs checker confirms tracked adoption/index (0 historical managed documents); workflow structure, relative index links and diff checks pass. No managed governance checker exists in this legacy repository. CI uses the immutable standard checkout with persist-credentials disabled and the existing organization secret reference; no secret value was read.
